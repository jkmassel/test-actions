name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: echo "::set-output name=items::strawberry,banana,carrot"
  do-stuff:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        language: ["en-US", "es-ES"]
    steps:
     - uses: actions/checkout@v1
     - name: Install Bundler
       run: echo ${{ items }}

